An exception is an object that represents an error that occurs during the execution of a program, allowing developers to handle errors gracefully using structured error handling techniques.

Exceptions are derived from the `System.Exception` class and can be managed using `try`, `catch`, and `finally` blocks to ensure that errors are properly addressed without crashing the application.


Few exception types:
- ArgumentException - occurs when a method receives an argument that is not valid